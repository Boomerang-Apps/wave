# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ═══════════════════════════════════════════════════════════════════════════════
# Validation Mode Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Validated: GitLab CI/CD best practices
# Options: strict | dev | ci
#   strict - Full validation for production (95% pass rate, all checks enabled)
#   dev    - Fast iteration for development (70% pass rate, behavioral probes skipped)
#   ci     - CI/CD pipeline mode (95% pass rate, optimized for automation)
WAVE_VALIDATION_MODE=dev

# ═══════════════════════════════════════════════════════════════════════════════
# Slack Integration (Generic - Project Agnostic)
# ═══════════════════════════════════════════════════════════════════════════════
# Validated: Slack Developer Docs - https://docs.slack.dev/reference/methods/chat.postMessage/
#
# SETUP INSTRUCTIONS (Step 2.2 - Manual):
# 1. Go to https://api.slack.com/apps
# 2. Click "Create New App" → "From Scratch"
# 3. Name: "WAVE Pipeline Notifier" (or your project name)
# 4. Select your workspace
# 5. Go to "OAuth & Permissions" → Add these Bot Token Scopes:
#    - chat:write        (Post messages)
#    - chat:write.public (Post to public channels without joining)
#    - reactions:write   (Add reactions - optional)
# 6. Click "Install to Workspace"
# 7. Copy the "Bot User OAuth Token" (starts with xoxb-)
# 8. Get Channel IDs: Open Slack, right-click channel → "View channel details" → scroll to bottom
#
# ═══════════════════════════════════════════════════════════════════════════════

SLACK_ENABLED=true
PROJECT_NAME=MyProject

# Slack Web API (RECOMMENDED - enables threading)
# Validated: thread_ts MUST be string, use Channel IDs not names
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_CHANNEL_UPDATES=C0123456789    # Channel ID for story updates (thread-per-story)
SLACK_CHANNEL_ALERTS=C0123456790     # Channel ID for critical alerts
SLACK_CHANNEL_BUDGET=C0123456791     # Channel ID for budget warnings

# Legacy Webhook (FALLBACK - no threading support)
# Use this if you don't want to set up OAuth
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
